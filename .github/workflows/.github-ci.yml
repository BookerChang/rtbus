name: Test Local Runner

on:
  push:
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Show runner information
        run: |
          echo "Hello from local runner"
          whoami
          hostname
          uname -a
          pwd
